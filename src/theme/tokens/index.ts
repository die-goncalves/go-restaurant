import { colors } from './colors'
import { easings } from './easings'
import { fonts } from './fonts'
import { zIndexes } from './z-indexes'

const tokens = { colors, easings, fonts, zIndexes }
export default tokens
